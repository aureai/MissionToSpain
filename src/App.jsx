import Roadmap from "./Roadmap.jsx";

/**
 * App shell only imports Roadmap. To swap the whole UI, replace src/Roadmap.jsx
 * (or point the import below at another file that default-exports the same component).
 */
export default function App() {
  return <Roadmap />;
}
